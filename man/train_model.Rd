% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_train_test_predict.R
\name{train_model}
\alias{train_model}
\title{Train Model on Training Data Only}
\usage{
train_model(
  train_data,
  n_epochs = 200,
  bootstrap_B = 150,
  k_folds = 5,
  variance_threshold = 0.95,
  verbose = TRUE
)
}
\arguments{
\item{train_data}{Training data frame from \code{\link{train_test_split}}.}

\item{n_epochs}{Integer. GD epochs. Default 200.}

\item{bootstrap_B}{Integer. Bootstrap resamples. Default 150.}

\item{k_folds}{Integer. CV folds (on training data only). Default 5.}

\item{variance_threshold}{Numeric. PCA variance. Default 0.95.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
A trained pipeline list (same as \code{\link{full_pipeline}}).
}
\description{
Runs the full pipeline (PCA + EM + GD + kNN + Bootstrap + CLT + CV)
exclusively on the training set. The returned model object is then
used to predict on the unseen test set via \code{\link{predict_on_test}}.
}
\examples{
\dontrun{
data    <- load_creditcard()
splits  <- train_test_split(data)
trained <- train_model(splits$train)
results <- predict_on_test(trained, splits$test)
}

}
