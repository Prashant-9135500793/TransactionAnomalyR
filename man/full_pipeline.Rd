% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_cross_validation_pipeline.R
\name{full_pipeline}
\alias{full_pipeline}
\title{Run the Full Fraud Detection Pipeline}
\usage{
full_pipeline(
  data,
  n_epochs = 200,
  bootstrap_B = 150,
  k_folds = 5,
  variance_threshold = 0.95,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame from \code{\link{load_creditcard}}.}

\item{n_epochs}{Integer. Gradient descent epochs. Default 200.}

\item{bootstrap_B}{Integer. Bootstrap resamples. Default 150.}

\item{k_folds}{Integer. Cross-validation folds. Default 5.}

\item{variance_threshold}{Numeric. PCA variance threshold. Default 0.95.}

\item{verbose}{Logical. Print progress messages. Default TRUE.}
}
\value{
A named list with all pipeline outputs:
  \code{prep}, \code{pca}, \code{em}, \code{gd}, \code{knn},
  \code{bootstrap}, \code{clt}, \code{cv}.
}
\description{
Convenience wrapper that runs all 7 steps in sequence:
preprocess → PCA → EM → Gradient Descent → kNN → Bootstrap → CLT → Cross-Validation.
}
\examples{
\dontrun{
data     <- load_creditcard(sample_size = 5000)
pipeline <- full_pipeline(data)
cat("AUC:", pipeline$cv$mean_auc, "\n")
launch_dashboard(pipeline)
}

}
